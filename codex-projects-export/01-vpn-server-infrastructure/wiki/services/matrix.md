# Matrix

На `www` размещены Matrix Synapse, nginx, coturn и Synapse Admin. Командный агент выполняет status/Fail2Ban/VPN-user операции через ограниченный SSH wrapper; AI bot работает отдельным сервисом. Matrix access tokens и содержимое `/etc/vpn-audit-alerts/matrix.conf` не импортируются.
